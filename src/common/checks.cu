// Translation unit for gpp/common/checks.cuh.
// The macros (CUDA_CHECK, CUDA_CHECK_LAST) are header-only.
// This file ensures the header compiles cleanly and gives a place
// for future runtime helpers (e.g. custom error callbacks).

#include "gpp/common/checks.cuh"
