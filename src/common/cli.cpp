// Translation unit for gpp/common/cli.hpp.
// parse_cli() is currently header-only (inline).
// This file ensures the header compiles cleanly and gives a place
// for future helpers (e.g. config-file loading, JSON size sweeps).

#include "gpp/common/cli.hpp"
